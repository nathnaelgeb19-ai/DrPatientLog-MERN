import {Router} from 'express';import {Setting,AuditLog} from '../models/index.js';import {auth,adminOnly} from '../middleware/auth.js';const r=Router();r.use(auth);r.get('/',async(q,s)=>{const rows=await Setting.find().lean();s.json(Object.fromEntries(rows.map(x=>[x.key,x.value])))});r.put('/',adminOnly,async(q,s)=>{for(const [key,value] of Object.entries(q.body||{}))await Setting.updateOne({key},{key,value:String(value)},{upsert:true});await AuditLog.create({doctorId:q.doctor._id,doctorName:q.doctor.name,action:'update',entity:'settings',detail:JSON.stringify(q.body)});s.json({ok:true})});export default r;
